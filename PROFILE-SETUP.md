# GitHub UI steps this connector still cannot do

File commits now work. Bio, pins, About boxes, renames, and archive still need github.com.

## 1. Bio — https://github.com/settings/profile

```
Software Engineer @ UBS · XLRI Finance · Amazon AIdeaS 2026 · cloud-native by day, AI products at night
```

Tick **Available for hire** if that is still true.

## 2. Pins — Customize your pins on https://github.com/SamyakJ05

1. `SEO-Optimisation` (SignalScan)
2. `Email-Verifier`
3. `ProofBoard` (or `F1` for Apex Atlas)
4. `trader`

Do not pin Portfolio, Portfolio-3D, or forks.

## 3. DeckIQ About box

Change description to: `AI pitch-deck critique for the IBM Bob Hackathon. Not RetainIQ.`
Remove the retainiq.cloud homepage from DeckIQ.

## 4. Rename when convenient

- `F1` → `apex-atlas`
- `SEO-Optimisation` → `signalscan`

GitHub keeps redirects. Then update the two links in this profile README.

## 5. Archive unused forks

`rich`, `vite`, `hatchet`, `cursive`, `napi-rs`, `rails_admin`, `cssnano`, `jsonschema`, `heroui`, `Scrapling`, `http-proxy-3` unless you have an open upstream PR.
